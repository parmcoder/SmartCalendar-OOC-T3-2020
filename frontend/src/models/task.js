export default class User {
    constructor(tid, title, description, dateStart, dateEnd) {
        this.tid = tid;
        this.title = title;
        this.description = description;
        this.dateStart = dateStart;
        this.dateEnd = dateEnd;
    }
}
